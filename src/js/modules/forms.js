const forms = () => {
    const form = document.querySelectorAll('form'), // получаем все формы на странице
        input = document.querySelectorAll('input') //получаем все инпуты на странице
};

export default forms;